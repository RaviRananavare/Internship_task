{
  "name": "rate-limiter",
  "version": "1.0.0",
  "description": "IP-based rate limiting middleware with configurable request limits and time windows",
  "main": "rateLimiter.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  }
}